package com.example.switchyard.switchyard_example

global java.util.Map globals

rule "HelloGreetingService"
    when
        $name : String(this == "first")
    then
        globals.put("response", String.format("Modified Hello %s!", $name));
end
